#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdarg.h>

/*int
main()
{
        char * c = "this\nis a test\v";
		char * buf;
		int i;
		
		asprintf(&buf, "%s", c);
		
		for (i = 0; i < strlen(buf); i++);
			printf("%c", buf[i]);
		
		printf("%s", buf);
		
        return 0;
}*/
